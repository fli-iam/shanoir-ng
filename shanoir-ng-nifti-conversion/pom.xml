<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.shanoir-ng</groupId>
        <artifactId>shanoir-ng-back</artifactId>
        <version>2.6.6</version>
        <relativePath>../shanoir-ng-back</relativePath>
    </parent>
  <artifactId>nifti-conversion</artifactId>
  <packaging>jar</packaging>

  <name>shanoir-ng-nifti-conversion</name>
  <description>Shanoir-NG nifti conversion Microservice</description>
  
    <build>
        <plugins>
            <!-- Package shanoir-ng-nifti-conversion.jar as Spring Boot executable: Avoid error: 
                no main manifest attribute, in /shanoir-ng-nifti-converter.jar -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <classpathDependencyExcludes>
                        <classpathDependencyExclude>org.slf4j:slf4j-log4j12</classpathDependencyExclude>
                    </classpathDependencyExcludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <!-- Copy shanoir-ng-nifti-conversion.jar for docker-compose up running -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <configuration>
                            <target>
                                <copy file="target/${project.build.finalName}.jar"
                                    tofile="${basedir}/../docker-compose/nifti-conversion/${project.artifactId}.jar" />
                                <copy file="../utils/entrypoint_common"
                                    tofile="${basedir}/../docker-compose/nifti-conversion/entrypoint_common" />
                                <copy file="../utils/oneshot"
                                    tofile="${basedir}/../docker-compose/nifti-conversion/oneshot" />
                                <chmod perm="+x"
                                    file="${basedir}/../docker-compose/nifti-conversion/oneshot" />
                            </target>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
            <id>mvn-repo-master</id>
            <url>https://raw.github.com/nroduit/mvn-repo/master/</url>
        </repository>
    </repositories>

    <profiles>
        <profile>
            <id>dev</id>
            <properties>
                <spring-active-profile>dev</spring-active-profile>
            </properties>
        </profile>
        <profile>
            <id>qualif</id>
            <properties>
                <spring.active.profile>qualif</spring.active.profile>
                <maven.test.skip>true</maven.test.skip>
            </properties>
        </profile>
    </profiles>
</project>