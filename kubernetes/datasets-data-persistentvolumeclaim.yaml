apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: datasets-data
  name: datasets-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
