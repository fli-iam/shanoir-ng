apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: studies-data
  name: studies-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
