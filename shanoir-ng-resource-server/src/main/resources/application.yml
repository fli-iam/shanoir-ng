# server configuration
server:
  port: 9901
# spring security configuration
security:
  session: NEVER
# redis configuration
spring:
  redis:
    host: localhost
# logging configuration
logging:
  file: shanoir-ng-resource-server.log